let nome = "vitor emanuel"

for (const iterator of nome) {
    //para passar o valor de cada vetor dentro da memória da string tem que passar o parametro interator[i] envez da string
    console.log(iterator);
}